/**
 * Canonical renderer host contract.
 *
 * The legacy `electron-bridge` module remains available so existing product
 * sources can migrate without a synchronized breaking release.
 */
export * from './electron-bridge';
